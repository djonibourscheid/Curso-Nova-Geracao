// Padronize para ter maior consistência nos nossos dados.
const nomes = ["ana Julia", "Caio vinicius", "BIA silva"];

const nomesAtualizados = nomes.map((nome) => nome.toUpperCase());

console.log(nomesAtualizados);